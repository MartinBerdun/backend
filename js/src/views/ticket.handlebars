{{!-- <div id="app">
  <nav >
    <ul>
      <li>
        <a href="/products">
          <button
          >Home 🏚️</button>
        </a>
      </li>
      <li>
        <a href="/tickets">
          <button
          >My Orders 🎫</button>
        </a>
      </li>
    </ul>
  </nav>
  <h1>Your
    Orders</h1>
  {{#if tickets}}
    <div id="ticketsDetail">
      {{#each tickets}}
        <div id="{{this._id}}">
          <div>
            <div>
              <h5>Order code: {{this.code}}</h5>
              <p>Purchase date:
                {{this.purchase_datetime}}</p>
              {{! ciclo 2 }}
                {{!-- {{#each this.products}}
                    <p>{{this.quantity}}
                    x
                    {{this.productId.title}}
                    - $
                    {{this.productId.price}}
                    ea.</p>
                {{/each}} --}}
            {{!-- </div>
          </div>
        </div>
      {{/each}}
    </div>
  {{else}}
{{/if}}

</div>
<div>
<br /><hr /><br />
</div>
<div>
<h3>Views</h3>
</div> --}}

{{!-- <script
  src="https://cdn.jsdelivr.net/npm/sweetalert2@11.7.3/dist/sweetalert2.all.min.js
"
></script> --}}

{{#if tickets  }}
<div class="ticket">
        <table>
            <thead>
                <tr>
                    <th>Code</th>
                    <th>Date & Time</th>
                    <th>Purchase</th>
                    <th>Amount</th>

                </tr>
            </thead>
            {{#each tickets}}
            <tbody>
                <tr>
                    <td>{{this.code}}</td>
                    <td>{{this.purchase_datetime}}</td>
                    <td>{{this.purchaser}}</td>
                    <td>$ {{this.ammount}}</td>

                </tr>
            </tbody>
            {{/each}}
        </table>
    </div>

{{/if }}

